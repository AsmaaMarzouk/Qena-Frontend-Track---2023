import { BorderBoxDirective } from './border-box.directive';

describe('BorderBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderBoxDirective();
    expect(directive).toBeTruthy();
  });
});
